# MonkeysLegion v2 — Telemetry Configuration

telemetry {
    enabled       = env("TELEMETRY_ENABLED", true)
    service_name  = env("TELEMETRY_SERVICE", "monkeyslegion")
    sampling_rate = 1.0

    metrics {
        enabled   = true
        namespace = "ml"
    }
}
